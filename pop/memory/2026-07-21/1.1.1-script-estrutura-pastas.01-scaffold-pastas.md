---
task: 1.1.1-script-estrutura-pastas
entry: 01-scaffold-pastas
---

# Scaffold de pastas de scripts

Criadas quatro pastas (`vod-scripts/`, `creed-scripts/`, `bin-scripts/`, `office-scripts/`) com scaffold homogêneo: `.env.example`, `setup_venv.sh` executável, `reports/.gitkeep` e `README.md` pt-BR. O `.gitignore` raiz cobre `.env`, `.venv/` e `reports/*`.

## Evidência

- [[dark-script/vod-scripts/setup_venv.sh|setup_venv.sh]] — *arquivo do scaffold*.
- [[dark-script/.gitignore|.gitignore raiz]] — *regras de ignore*.
