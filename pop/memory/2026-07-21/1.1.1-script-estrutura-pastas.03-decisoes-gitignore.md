---
task: 1.1.1-script-estrutura-pastas
entry: 03-decisoes-gitignore
---

# Decisões sobre .gitignore e requirements.txt

`requirements.txt` foi omitido até o primeiro pacote real; `setup_venv.sh` já trata a instalação condicionalmente. No `.gitignore` raiz usou-se `**/reports/*` + `!**/reports/.gitkeep` para casar reports em qualquer profundidade. Nit: padrão sem barra inicial já casa em profundidade (simplificação futura opcional).

## Evidência

- [[dark-script/.gitignore|.gitignore raiz]] — *regra de reports discutida*.
- [[dark-script/vod-scripts/setup_venv.sh|setup_venv.sh]] — *setup que compensa a ausência de requirements.txt*.
