---
task: 1.2.2-script-vod-fix-faststart
project: applications/dark-store/dark-script
started: 2026-07-23
finished: 2026-07-23
commit: 123a857
pr:
---

# 1.2.2-script-vod-fix-faststart — Correção de VODs sem faststart

- **Entrega:** `vod-scripts/fix_faststart.py` — corrige `.mp4`/`.mkv` de `SCAN_VOD_FOLDER` sem faststart, com modos interativo (backup `.bak`, default `Y`), `--yes` (sem backup) e `--dry-run`; inclui verificação embutida; gera `reports/faststart-fix-errors-YYYYMMDDHHMM.md` apenas com erros; exit 0 em execução normal, 2 em falha operacional.
- **Refatoração:** extraído módulo compartilhado `vod-scripts/vod_faststart.py` com funções de detecção reutilizadas por `report_faststart.py` e `fix_faststart.py`.
- **Ajustes:** `vod-scripts/setup_venv.sh` agora usa `--upgrade` no `pip install -r requirements.txt` quando `.venv` já existe; `vod-scripts/README.md` atualizado com prerequisitos e uso do novo script.
- **Áreas alteradas:** `vod-scripts/fix_faststart.py`, `vod-scripts/vod_faststart.py`, `vod-scripts/report_faststart.py`, `vod-scripts/setup_venv.sh`, `vod-scripts/README.md`, `pop/roadmap/1-gerencia-de-vod.md`.
- **Verificação:** testado com fixtures ffmpeg (MP4 com/sem faststart), modos `--dry-run`, `--yes` e interativo; subpastas; erro de backup existente. Teste de MKV depende de `mkvmerge` (MKVToolNix), listado como prerequisito.
- **Decisões duráveis:** correção MP4 via `ffmpeg -c copy -movflags +faststart`; correção MKV via `mkvmerge -o`; substituição atômica na mesma pasta; permissões preservadas com `shutil.copymode`; sem backup no modo `--yes`; `mkvmerge` verificado no momento do uso (lazy), não como prerequisite estrito de startup.
- **Integração:** commit em develop e merge direto em main.

## Links

- **Script anterior:** [[1.2.1-script-vod-report-faststart]] — *detecção de faststart que deu origem às funções compartilhadas*.
