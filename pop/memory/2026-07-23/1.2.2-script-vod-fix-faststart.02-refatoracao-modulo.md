---
task: 1.2.2-script-vod-fix-faststart
entry: 02-refatoracao-modulo
---

# Refatoração do módulo vod_faststart

Extraído módulo compartilhado `vod-scripts/vod_faststart.py` com funções de detecção reutilizadas por `report_faststart.py` e `fix_faststart.py`. Atualizado `vod-scripts/setup_venv.sh` para usar `--upgrade` ao reinstalar dependências em `.venv` existente e `vod-scripts/README.md` com prerequisitos e uso do novo script.

## Evidência

- [[vod-scripts/vod_faststart.py]] — *módulo compartilhado extraído*.
- [[vod-scripts/setup_venv.sh]] — *upgrade de dependências no venv existente*.
- [[vod-scripts/README.md]] — *documentação atualizada*.
