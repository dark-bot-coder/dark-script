---
task: 1.2.2-script-vod-fix-faststart
entry: 01-fix-faststart
---

# Implementação de fix_faststart.py

Criado `vod-scripts/fix_faststart.py` para corrigir arquivos `.mp4` e `.mkv` de `SCAN_VOD_FOLDER` que não possuem faststart. Suporta modo interativo com backup `.bak` (default `Y`), `--yes` sem backup e `--dry-run`. Inclui verificação embutida, gera `reports/faststart-fix-errors-YYYYMMDDHHMM.md` apenas com erros, e retorna exit 0 ou 2.

## Evidência

- [[vod-scripts/fix_faststart.py]] — *script de correção implementado*.
