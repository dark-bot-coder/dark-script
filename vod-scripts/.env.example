# Variáveis de ambiente da família vod-scripts.
# Copie este arquivo para `.env` e preencha os valores antes de rodar os scripts.
# Nunca versione o `.env` (já ignorado no .gitignore da raiz).

# Pasta raiz onde os arquivos VOD serão varridos.
SCAN_VOD_FOLDER=
