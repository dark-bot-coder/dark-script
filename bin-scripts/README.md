# bin-scripts

Scripts da família Bin, rodando em servidores externos e enviando dados ao Dark Store.

## Setup

1. Crie o ambiente virtual: `./setup_venv.sh`
2. Ative-o: `source .venv/bin/activate`
3. Copie `.env.example` para `.env` e preencha os valores.

## Scripts

Nenhum script ainda.
